.container
  h1 Sign up for an account
  p Schoolhouse Yoga supports Google-integrated logins (those who have Gmail) and our own accounts.
  hr
  .row
    .col-sm-12.col-md-4.col-md-offset-2
      .panel.panel-primary
        .panel-heading
          h3.panel-title Setup a Google-integrated login
        .panel-body
          google-button
    .col-sm-12.col-md-4
      .panel.panel-primary
        .panel-heading
          h3.panel-title Setup a SHY login
        .panel-body
          form.form(name='form', ng-submit='$ctrl.register(form)', novalidate='', ng-model-options="{'updateOn': 'default blur', 'debounce': {'default': 200, 'blur': 0}}", autocomplete='on')
            .form-group(ng-class='{ "has-success": form.firstName.$valid && $ctrl.submitted,\
            "has-error": form.firstName.$invalid && $ctrl.submitted }')
              label First name
              input.form-control(type='text', name='firstName', ng-model='$ctrl.user.firstName', required='', autofocus='', autocomplete='given-name')
              p.help-block(ng-show='form.firstName.$error.required && $ctrl.submitted')
                | Your first name is required
            .form-group(ng-class='{ "has-success": form.lastName.$valid && $ctrl.submitted,\
            "has-error": form.lastName.$invalid && $ctrl.submitted }')
              label Last name
              input.form-control(type='text', name='lastName', ng-model='$ctrl.user.lastName', required='', autocomplete='family-name')
              p.help-block(ng-show='form.lastName.$error.required && $ctrl.submitted')
                | Your last name is required

            .form-group(ng-class='{ "has-success": form.email.$valid && $ctrl.submitted,\
            "has-error": form.email.$invalid && $ctrl.submitted }')
              label Email
              input.form-control(type='email', name='email', ng-model='$ctrl.user.email', required='', autocomplete='email', sequelize-error='')
              p.help-block(ng-show='form.email.$error.email && $ctrl.submitted')
                | Doesn't look like a valid email.
              p.help-block(ng-show='form.email.$error.required && $ctrl.submitted')
                | What's your email address?
              p.help-block(ng-show='form.email.$error.sequelize')
                | {{ $ctrl.errors.email }}

            .form-group(ng-class='{ "has-success": form.passwordNew.$valid && $ctrl.submitted,\
            "has-error": form.passwordNew.$invalid && $ctrl.submitted }')
              label Password
              input.form-control(type='password', name='passwordNew', ng-model='$ctrl.user.passwordNew', sequelize-error='', ng-minlength='3', maxlength='20', required='', autocomplete='new-password')
              p.help-block(ng-show='(form.passwordNew.$error.minlength || form.passwordNew.$error.required) && $ctrl.submitted')
                | Password must be at least 3 characters.
              p.help-block(ng-show='form.passwordNew.$error.sequelize')
                | {{ $ctrl.errors.passwordNew }}

            .form-group(ng-class='{ "has-success": form.passwordConfirm.$valid && $ctrl.submitted,\
            "has-error": form.passwordConfirm.$invalid && $ctrl.submitted }')
              label Confirm Password
              input.form-control(type='password', name='passwordConfirm', ng-model='$ctrl.user.passwordConfirm', compare-to='$ctrl.user.passwordNew', ng-minlength='3', maxlength='20', required='', autocomplete='new-password')
              p.help-block(ng-show='form.passwordConfirm.$invalid && $ctrl.submitted')
                | Passwords must match.

            .form-group(ng-class='{ "has-success": form.phone.$valid && $ctrl.submitted,\
            "has-error": form.phone.$invalid && $ctrl.submitted }')
              label Phone
              input.form-control(type='text', name='phone', ng-model='$ctrl.user.phone', required='', autocomplete='tel-national')
              p.help-block(ng-show='form.phone.$error.required && $ctrl.submitted')
                | Your phone number is required
            br
            div
              button.btn.btn-lg.btn-warning.pull-left(type='submit') Sign up
