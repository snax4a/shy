.container
  .row
    .col-sm-12.col-md-6.col-md-offset-3
      h1 Sign up for an account
      form.form(name='form', ng-submit='$ctrl.register(form)', novalidate='', ng-model-options="{'updateOn': 'default blur', 'debounce': {'default': 200, 'blur': 0}}")
        .form-group(ng-class='{ "has-success": form.firstName.$valid && $ctrl.submitted,\
        "has-error": form.firstName.$invalid && $ctrl.submitted }')
          label First name
          input.form-control(type='text', name='firstName', ng-model='$ctrl.user.firstName', required='', autofocus='')
          p.help-block(ng-show='form.firstName.$error.required && $ctrl.submitted')
            | Your first name is required
        .form-group(ng-class='{ "has-success": form.lastName.$valid && $ctrl.submitted,\
        "has-error": form.lastName.$invalid && $ctrl.submitted }')
          label Last name
          input.form-control(type='text', name='lastName', ng-model='$ctrl.user.lastName', required='')
          p.help-block(ng-show='form.lastName.$error.required && $ctrl.submitted')
            | Your last name is required

        .form-group(ng-class='{ "has-success": form.email.$valid && $ctrl.submitted,\
        "has-error": form.email.$invalid && $ctrl.submitted }')
          label Email
          input.form-control(type='email', name='email', ng-model='$ctrl.user.email', required='', autocomplete='on', sequelize-error='')
          p.help-block(ng-show='form.email.$error.email && $ctrl.submitted')
            | Doesn't look like a valid email.
          p.help-block(ng-show='form.email.$error.required && $ctrl.submitted')
            | What's your email address?
          p.help-block(ng-show='form.email.$error.sequelize')
            | {{ $ctrl.errors.email }}

        .form-group(ng-class='{ "has-success": form.password.$valid && $ctrl.submitted,\
        "has-error": form.password.$invalid && $ctrl.submitted }')
          label Password
          input.form-control(type='password', name='password', ng-model='$ctrl.user.password', sequelize-error='', ng-minlength='3', maxlength='20', required='')

          p.help-block(ng-show='(form.password.$error.minlength || form.password.$error.required) && $ctrl.submitted')
            | Password must be at least 3 characters.
          p.help-block(ng-show='form.password.$error.sequelize')
            | {{ $ctrl.errors.password }}

        .form-group(ng-class='{ "has-success": form.confirmPassword.$valid && $ctrl.submitted,\
        "has-error": form.confirmPassword.$invalid && $ctrl.submitted }')
          label Confirm Password
          input.form-control(type='password', name='confirmPassword', ng-model='$ctrl.user.confirmPassword', compare-to='$ctrl.user.password', ng-minlength='3', maxlength='20', required='')
          p.help-block(ng-show='form.confirmPassword.$invalid && $ctrl.submitted')
            | Passwords must match.

        .form-group(ng-class='{ "has-success": form.phone.$valid && $ctrl.submitted,\
        "has-error": form.phone.$invalid && $ctrl.submitted }')
          label Phone
          input.form-control(type='text', name='phone', ng-model='$ctrl.user.phone', required='')
          p.help-block(ng-show='form.phone.$error.required && $ctrl.submitted')
            | Your phone number is required

        br
        div
          button.btn.btn-lg.btn-warning.pull-left(type='submit') Sign up
          oauth-buttons
          //- a.btn.btn-default.btn-lg.btn-login(href='/login')
            | Login
