.container
  .row
    .col-sm-12.col-md-6.col-md-offset-3
      h2 Sign up for an account
      form.form(name='form', ng-submit='$ctrlregister(form)', novalidate='', ng-model-options="{'updateOn': 'default blur', 'debounce': {'default': 200, 'blur': 0}}")
        .form-group(ng-class='{ "has-success": form.firstName.$valid && $ctrlsubmitted,\
        "has-error": form.firstName.$invalid && $ctrlsubmitted }')
          label First name
          input.form-control(type='text', name='firstName', ng-model='$ctrluser.firstName', required='', autofocus='')
          p.help-block(ng-show='form.firstName.$error.required && $ctrlsubmitted')
            | Your first name is required
        .form-group(ng-class='{ "has-success": form.lastName.$valid && $ctrlsubmitted,\
        "has-error": form.lastName.$invalid && $ctrlsubmitted }')
          label Last name
          input.form-control(type='text', name='lastName', ng-model='$ctrluser.lastName', required='')
          p.help-block(ng-show='form.lastName.$error.required && $ctrlsubmitted')
            | Your last name is required

        .form-group(ng-class='{ "has-success": form.email.$valid && $ctrlsubmitted,\
        "has-error": form.email.$invalid && $ctrlsubmitted }')
          label Email
          input.form-control(type='email', name='email', ng-model='$ctrluser.email', required='', autocomplete='on', sequelize-error='')
          p.help-block(ng-show='form.email.$error.email && $ctrlsubmitted')
            | Doesn't look like a valid email.
          p.help-block(ng-show='form.email.$error.required && $ctrlsubmitted')
            | What's your email address?
          p.help-block(ng-show='form.email.$error.sequelize')
            | {{ $ctrlerrors.email }}

        .form-group(ng-class='{ "has-success": form.password.$valid && $ctrlsubmitted,\
        "has-error": form.password.$invalid && $ctrlsubmitted }')
          label Password
          input.form-control(type='password', name='password', ng-model='$ctrluser.password', sequelize-error='', ng-minlength='3', maxlength='20', required='')

          p.help-block(ng-show='(form.password.$error.minlength || form.password.$error.required) && $ctrlsubmitted')
            | Password must be at least 3 characters.
          p.help-block(ng-show='form.password.$error.sequelize')
            | {{ $ctrlerrors.password }}

        .form-group(ng-class='{ "has-success": form.confirmPassword.$valid && $ctrlsubmitted,\
        "has-error": form.confirmPassword.$invalid && $ctrlsubmitted }')
          label Confirm Password
          input.form-control(type='password', name='confirmPassword', ng-model='$ctrluser.confirmPassword', compare-to='$ctrluser.password', ng-minlength='3', maxlength='20', required='')
          p.help-block(ng-show='form.confirmPassword.$invalid && $ctrlsubmitted')
            | Passwords must match.

        .form-group(ng-class='{ "has-success": form.phone.$valid && $ctrlsubmitted,\
        "has-error": form.phone.$invalid && $ctrlsubmitted }')
          label Phone
          input.form-control(type='text', name='phone', ng-model='$ctrluser.phone', required='')
          p.help-block(ng-show='form.phone.$error.required && $ctrlsubmitted')
            | Your phone number is required

        br
        div
          button.btn.btn-lg.btn-warning.pull-left(type='submit') Sign up
          oauth-buttons
          //- a.btn.btn-default.btn-lg.btn-login(ui-sref='login')
            | Login
