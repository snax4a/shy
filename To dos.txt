High
- Server / mail sending from /subscribe, /contact and /order
- Server /subscribe with google-spreadsheet (https://www.npmjs.com/package/google-spreadsheet) to project to take subscriber info
- Server /contact method (leverages /subscribe and nodemailer)
- Server /order method with PayPal PayFlow Pro or Braintree

Low/cosmetic
- Maybe revisit how I create the tweet button (use JavaScript instead of HTML)
- Consider wrapping main page's sections in .col-md-6 so that they'll wrap